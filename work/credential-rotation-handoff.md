# Allow Credential Rotation Handoff

Generated: 2026-06-09T17:45:16.136Z
Status: ready_for_owner_rotation
Incident: credential-exposure-2026-06-09
Incident Status: needs_owner_rotation
Exposures: 2
Unresolved: 2

## Safety Boundary

- This handoff is local review material only.
- It does not move funds, revoke tokens, store secrets, store replacement credentials, post content, send outreach, sign wallet payloads, deploy contracts, start runtime, start pilot traffic, update canonical state, or enable a token.
- The owner performs rotation in wallet/provider interfaces and records only redacted proof references.

## Incident Gaps

- secret exposure incident: exposures[0].contained.fundsMovedToFreshWallet must be true
- secret exposure incident: exposures[0].contained.oldWalletNoLongerUsed must be true
- secret exposure incident: exposures[0].contained.replacementStoredOutsideRepo must be true
- secret exposure incident: exposures[1].contained.tokenRevoked must be true
- secret exposure incident: exposures[1].contained.replacementStoredOutsideRepo must be true

## Owner Actions

### Move funds out of Base wallet funding key

- id: `wallet_move_funds_1`
- type: `wallet_funds_move`
- mode: `owner_only`
- Create or choose a fresh wallet controlled outside this repository.
- Transfer all funds from the exposed wallet to the fresh wallet using your wallet app.
- Do not paste private keys, seed phrases, signed payloads, transaction hashes with sensitive notes, or wallet screenshots containing secrets into chat or repo files.
- Record only a redacted proof reference after the transfer is complete.
- redacted incident update:
```json
{
  "contained": {
    "fundsMovedToFreshWallet": true,
    "oldWalletNoLongerUsed": true,
    "replacementStoredOutsideRepo": true
  },
  "proof": {
    "ref": "redacted-wallet-transfer-proof",
    "capturedAt": "ISO-8601 timestamp",
    "redacted": true
  }
}
```

### Retire Base wallet funding key from Allow Protocol

- id: `wallet_retire_1`
- type: `wallet_retirement`
- mode: `owner_only`
- Remove the exposed wallet from future Allow Protocol handoff manifests.
- Replace it with only the fresh public wallet address and public readiness fields.
- Keep replacement wallet secrets in a local wallet or secret manager, outside this repository.
- redacted incident update:
```json
{
  "contained": {
    "oldWalletNoLongerUsed": true,
    "replacementStoredOutsideRepo": true
  }
}
```

### Revoke X account bearer token

- id: `x_token_revoke_2`
- type: `x_token_revocation`
- mode: `owner_only`
- Open the X developer portal as the account owner.
- Revoke or regenerate the exposed bearer token.
- Store any replacement token only in a local secret manager or deployment provider.
- Do not paste replacement tokens into chat, repo files, approval packets, or execution evidence.
- Record only a redacted revocation proof reference after the token is revoked.
- redacted incident update:
```json
{
  "contained": {
    "tokenRevoked": true,
    "replacementStoredOutsideRepo": true
  },
  "proof": {
    "ref": "redacted-x-token-revocation-proof",
    "capturedAt": "ISO-8601 timestamp",
    "redacted": true
  }
}
```

## Commands

- actionPack: `npm run credential-rotation-action-pack -- ops/secret_exposure_incident.template.json`
- validateIncident: `npm run secret-exposure-response -- ops/secret_exposure_incident.template.json`
- liveResourceHandoff: `npm run live-resource-handoff`
- readiness: `npm run readiness`
- operate: `npm run operate`

## Acceptance Criteria

- Every replacement credential is created and stored outside this repository.
- Funds are moved by the wallet owner to a fresh wallet before the exposed wallet is retired.
- The exposed X/API token is revoked or regenerated by the account owner before any posting approval.
- The incident record contains only redacted proof references and ISO-8601 timestamps.
- secret-exposure-response, live-resource-handoff, readiness, and operate all run after the redacted update.

## Warnings

- credential rotation action pack: exposures[0] remains unresolved
- credential rotation action pack: exposures[1] remains unresolved

## Next Action

Owner completes 3 outside-repo rotation actions, updates only redacted incident proof fields, then runs the post-rotation commands.
